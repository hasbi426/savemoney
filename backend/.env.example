PORT=5001 # Or any port you prefer for the backend
DB_HOST=localhost
DB_USER=your_postgres_user
DB_PASSWORD=your_postgres_password
DB_NAME=savemoney_db
DB_PORT=5432
DB_DIALECT=postgres

JWT_SECRET=YOUR_VERY_STRONG_JWT_SECRET_KEY_HERE
JWT_EXPIRES_IN=1h

# For Sequelize CLI (if different from runtime, usually the same)
# DEVELOPMENT_DB_USERNAME=${DB_USER}
# DEVELOPMENT_DB_PASSWORD=${DB_PASSWORD}
# DEVELOPMENT_DB_NAME=${DB_NAME}
# DEVELOPMENT_DB_HOST=${DB_HOST}
# DEVELOPMENT_DB_DIALECT=${DB_DIALECT}